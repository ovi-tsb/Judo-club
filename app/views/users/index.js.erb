<% js = escape_javascript(
  
  
    render(
      :partial => 'users/user',
      :locals => { :users => @users },
      :locals => { users: @users }
    )
  

    
) %>
$("#filterrific_results").html("<%= js %>");